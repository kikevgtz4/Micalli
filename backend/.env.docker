# Database - Docker PostgreSQL
DATABASE_URL=postgres://postgres:postgres@db:5432/unihousing
POSTGRES_HOST=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=unihousing

# Redis - Docker Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend,0.0.0.0

# IMPORTANT: This tells Django we're in Docker
IN_DOCKER=true

# Email Settings (same as local)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=evgtz4@gmail.com
EMAIL_HOST_PASSWORD=gsyc exjb yvkr mtsu
DEFAULT_FROM_EMAIL=UniHousing <evgtz4@gmail.com>

# Frontend URL
FRONTEND_URL=http://localhost:3000

# JWT Settings
JWT_SECRET_KEY=your_jwt_secret_key

# Cloudinary (same as local)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret